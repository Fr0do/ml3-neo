"""Evaluation script for hw 07-pretrain. Stub for judge-only homeworks."""

def evaluate_submission(submission_dir, hidden_data_dir):
    # This is a pure judge homework, metric score is 0 and not used.
    # We just return a dummy dictionary so the pipeline doesn't crash.
    return {"accuracy": 0.0}
